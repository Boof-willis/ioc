import { ShieldCheck as ShieldCheckIcon } from 'lucide-react';

export default function ShieldCheck({ className = "w-12 h-12" }: { className?: string }) {
  return <ShieldCheckIcon className={className} />;
}

