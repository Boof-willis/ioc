import { AlertTriangle as AlertTriangleIcon } from 'lucide-react';

export default function AlertTriangle({ className = "w-8 h-8" }: { className?: string }) {
  return <AlertTriangleIcon className={className} />;
}

