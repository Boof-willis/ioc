import { UserCheck as UserCheckIcon } from 'lucide-react';

export default function UserCheck({ className = "w-6 h-6" }: { className?: string }) {
  return <UserCheckIcon className={className} />;
}

