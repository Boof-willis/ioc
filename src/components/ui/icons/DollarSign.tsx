import { DollarSign as DollarSignIcon } from 'lucide-react';

export default function DollarSign({ className = "w-12 h-12" }: { className?: string }) {
  return <DollarSignIcon className={className} />;
}

