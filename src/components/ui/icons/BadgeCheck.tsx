import { BadgeCheck as BadgeCheckIcon } from 'lucide-react';

export default function BadgeCheck({ className = "w-12 h-12" }: { className?: string }) {
  return <BadgeCheckIcon className={className} />;
}

